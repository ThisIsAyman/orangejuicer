# Base URL of the stateless OTF relay proxy (Cloudflare Worker).
# Required for the hosted SPA to fetch live data (works around OTF's CORS).
# Leave unset only for the browser-extension / desktop build (DirectTransport).
VITE_OTF_PROXY_URL=https://otf-proxy.your-subdomain.workers.dev
